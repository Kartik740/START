# START — Environment Configuration
# Optional: Connect your Supabase project for remote PostgreSQL & Auth synchronization.
# If omitted, START operates in local-first reactive storage mode.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Backend (Server-Only) Environment Variables - MUST NEVER BE PREFIXED WITH VITE_
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Optional: Google Gemini API key for pragmatic anti-procrastination AI Coach reasoning
# (task decomposition, obstacle contingency if-then plans).
VITE_GEMINI_API_KEY=
